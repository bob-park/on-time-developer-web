services:
  web:
    image: ghcr.io/bob-park/on-time-developer-web:${VERSION}
    build:
      context: ./
      dockerfile: ./Dockerfile
      args:
        - GITHUB_NPM_AUTH_TOKEN=${GITHUB_NPM_AUTH_TOKEN}
      x-bake:
        platforms:
          - linux/amd64
          - linux/arm64
        tags:
          - ghcr.io/bob-park/on-time-developer-web:${VERSION}
          - ghcr.io/bob-park/on-time-developer-web:latest
